<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Notification permission (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Schedule exact alarms for class/exam reminders (API 33+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name=".MyHebnuApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyHEBNU"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MyHEBNU"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Widget deep link: myhebnu://navigate/{route} -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <data android:scheme="myhebnu" android:host="navigate" />
            </intent-filter>
        </activity>

        <!-- Widget: 课表·微型 (2×2) -->
        <receiver
            android:name=".widget.MicroWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_micro_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/schedule_widget_micro" />
        </receiver>

        <!-- Widget: 课表·横条 (4×2) -->
        <receiver
            android:name=".widget.MediumWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_medium_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/schedule_widget_medium" />
        </receiver>

        <!-- Widget: 课表·日历 (4×4) -->
        <receiver
            android:name=".widget.LargeGridReceiver"
            android:exported="false"
            android:label="@string/widget_large_grid_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/schedule_widget_large_grid" />
        </receiver>

        <!-- Widget: 课表·列表 (4×4) -->
        <receiver
            android:name=".widget.LargeListReceiver"
            android:exported="false"
            android:label="@string/widget_large_list_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/schedule_widget_large_list" />
        </receiver>

        <!-- Grid navigation broadcast receiver (internal) -->
        <receiver
            android:name=".widget.GridNavReceiver"
            android:exported="false" />
    </application>
</manifest>
